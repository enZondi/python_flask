services:
  - type: web
    name: apex-login-wrapper
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app/main.py"
    envVars:
      - key: PORT
        value: "10000"
